# Criar um script, que vai pegar a data atual, subtrair dois anos, e baixar todos os arquivos de dentro das pastas
# que o numeros da pasta seja maior ou igual a este ano subtraido.
# Criar uma função que vai esse monte de arquivos em zip, e descompatar eles.
# Configurar um banco usando o SQLAlchemy, e configurar as credenciais do banco, usando algo parecido
# com o .env, usando o docerfile.
# Criar duas tables, a primeira contendo aas colunas do primeiro arquivo, mesma coisa para o segundo arquivo.
# criar um script, que vai iterar os arquivos do mais velho para o mais recente, e despejar
# essas informações no banco configurado.
# configurar o datagrip: 1- Abrir o + e criar um  datasource usando o postgreSQL, 2- colocar o nome do banco, usuario e senha,
# 3- clicar em test para ver se está tudo certo, 4- clicar em apply.
# Clicar no quinto botão. E criar duas query usando o SQL.
# Usar o SQLAlchemy usando as querys, e salvar nas variaveis.
# Retornar este valor.